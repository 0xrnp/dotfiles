services:
  redis:
    image: redis:latest
    container_name: redis
    restart: always
    ports:
      - "6379:6379"
    command: redis-server
    volumes:
      - redis-data:/data
    networks:
      - redis-net

volumes:
  redis-data:
    driver: local

networks:
  redis-net:
    driver: bridge
