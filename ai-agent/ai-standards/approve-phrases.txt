# Compatibility copy of the portable approval phrases in AGENTS.md.
# Agents must read the inline list in AGENTS.md, rather than rely on this path.
# Case-insensitive. The last non-empty line of the user message must equal one
# token. Earlier lines may stack other control tokens (WORKTREE, MAIN, NO
# TICKET, ticket ids). Valid for the current turn only.
gooo

proceed

approve

goahead

# This is a separate control token. It skips the plan write-up only when the
# requested scope is already exact. It is not an approval phrase.
noplan
