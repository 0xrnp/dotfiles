# Case-insensitive. The trimmed user message must equal one token.
# Valid for the current turn only. First token is primary.
gooo

okgo

# Skip the plan write-up only when the requested scope is already exact.
noplan
